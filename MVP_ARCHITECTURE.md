# WiseInvest MVP 아키텍처

## 1. MVP 범위 요약

| 항목 | MVP 범위 |
|------|----------|
| 시장 | 한국 주식만 |
| 분석 모델 | 워런 버핏 스타일 1개 |
| 데이터 입력 | 수동 입력 + CSV 업로드 |
| 핵심 화면 | 5개 (대시보드, 종목분석, 투자기록, 성향리포트, 관리자) |
| 권한 시스템 | admin/user (is_admin 부울 컬럼) |

---

## 2. 시스템 구성

```
┌──────────────────────────────────────────────────────┐
│                     웹 브라우저                        │
└──────────────────────────┬───────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────┐
│                  Rails 8 Application                  │
│                                                       │
│   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  │
│   │   Hotwire   │  │  Tailwind   │  │  Stimulus   │  │
│   └─────────────┘  └─────────────┘  └─────────────┘  │
│                                                       │
│   ┌─────────────────────────────────────────────┐    │
│   │              Controllers                     │    │
│   │  Dashboard │ Stocks │ Records │ Profiles    │    │
│   └─────────────────────────────────────────────┘    │
│                                                       │
│   ┌─────────────────────────────────────────────┐    │
│   │              Services                        │    │
│   │  BuffettAnalyzer │ ProfileService │ CsvParser│    │
│   └─────────────────────────────────────────────┘    │
│                                                       │
│   ┌─────────────────────────────────────────────┐    │
│   │            Solid Queue (Jobs)                │    │
│   └─────────────────────────────────────────────┘    │
└──────────────────────────┬───────────────────────────┘
                           │
              ┌────────────┴────────────┐
              ▼                         ▼
       ┌────────────┐           ┌────────────┐
       │ PostgreSQL │           │  외부 API   │
       │            │           │ (KIS/DART) │
       └────────────┘           └────────────┘
```

---

## 3. 디렉토리 구조 (MVP)

```
wiseinvest/
├── app/
│   ├── controllers/
│   │   ├── application_controller.rb
│   │   ├── sessions_controller.rb
│   │   ├── registrations_controller.rb
│   │   ├── dashboard_controller.rb
│   │   ├── stocks_controller.rb
│   │   ├── investment_records_controller.rb
│   │   ├── profiles_controller.rb
│   │   ├── concerns/
│   │   │   ├── authentication.rb
│   │   │   └── authorization.rb
│   │   └── admin/
│   │       ├── base_controller.rb
│   │       └── users_controller.rb
│   │
│   ├── models/
│   │   ├── user.rb
│   │   ├── stock.rb
│   │   ├── stock_metric.rb
│   │   ├── investment_record.rb
│   │   ├── investment_profile.rb
│   │   ├── survey_response.rb
│   │   └── analysis_report.rb
│   │
│   ├── services/
│   │   ├── buffett_analyzer.rb
│   │   ├── profile_calculator.rb
│   │   └── csv_import_service.rb
│   │
│   ├── clients/
│   │   └── kis_client.rb
│   │
│   ├── jobs/
│   │   └── stock_sync_job.rb
│   │
│   ├── views/
│   │   ├── layouts/
│   │   ├── dashboard/
│   │   ├── stocks/
│   │   ├── investment_records/
│   │   ├── profiles/
│   │   └── admin/
│   │       └── users/
│   │
│   └── javascript/
│       └── controllers/
│           ├── search_controller.js
│           └── rating_controller.js
│
├── config/
│   ├── routes.rb
│   └── database.yml
│
└── db/
    └── migrate/
```

---

## 4. 데이터베이스 스키마

### 4.1. ERD

```
┌─────────────┐     ┌─────────────────┐      ┌─────────────┐
│    users    │────▶│investment_profiles│    │   stocks    │
└─────────────┘     └─────────────────┘      └──────┬──────┘
      │                                             │
      │             ┌─────────────────┐             │
      └────────────▶│investment_records│◀───────────┘
                    └─────────────────┘
      │                                            │
      │             ┌─────────────────┐            │
      └────────────▶│survey_responses │     ┌──────┴──────┐
                    └─────────────────┘     │stock_metrics│
                                            └─────────────┘
                    ┌─────────────────┐            │
                    │analysis_reports │◀───────────┘
                    └─────────────────┘
```

### 4.2. 테이블 정의

```ruby
# users
create_table :users do |t|
  t.string :email, null: false
  t.string :password_digest, null: false
  t.string :name
  t.boolean :is_admin, default: false, null: false  # 관리자 여부
  t.datetime :suspended_at                           # 계정 정지 시각
  t.timestamps
end

# stocks
create_table :stocks do |t|
  t.string :symbol, null: false      # 종목코드
  t.string :name, null: false        # 종목명
  t.string :sector                   # 섹터
  t.timestamps
end

# stock_metrics (재무지표)
create_table :stock_metrics do |t|
  t.references :stock, null: false
  t.decimal :roe                     # 자기자본이익률
  t.decimal :debt_ratio              # 부채비율
  t.decimal :operating_margin        # 영업이익률
  t.decimal :per                     # PER
  t.decimal :pbr                     # PBR
  t.integer :profit_growth_years     # 연속 이익 증가 연수
  t.date :data_date
  t.timestamps
end

# investment_records
create_table :investment_records do |t|
  t.references :user, null: false
  t.references :stock, null: false
  t.string :action, null: false      # buy, sell
  t.integer :quantity, null: false
  t.decimal :price, null: false
  t.datetime :traded_at, null: false
  t.string :reason_tags, array: true, default: []
  t.integer :pre_rating              # 1-5 별점
  t.timestamps
end

# investment_profiles
create_table :investment_profiles do |t|
  t.references :user, null: false
  t.integer :survey_score            # 설문 기반 점수
  t.integer :actual_score            # 실제 행동 점수
  t.string :risk_tolerance           # conservative, moderate, aggressive
  t.jsonb :biases, default: {}       # 발견된 편향
  t.timestamps
end

# survey_responses
create_table :survey_responses do |t|
  t.references :user, null: false
  t.string :question_key, null: false
  t.integer :answer_value, null: false
  t.timestamps
end

# analysis_reports
create_table :analysis_reports do |t|
  t.references :stock, null: false
  t.integer :total_score, null: false
  t.jsonb :score_breakdown, default: {}
  t.datetime :expires_at
  t.timestamps
end
```

---

## 5. 핵심 서비스

### 5.1. 버핏 분석기

```ruby
# app/services/buffett_analyzer.rb
class BuffettAnalyzer
  CRITERIA = {
    roe:              { weight: 20, min: 15 },
    debt_ratio:       { weight: 15, max: 50 },
    operating_margin: { weight: 15, min: 10 },
    profit_growth:    { weight: 20, min: 5 },
    per:              { weight: 10, max: 15 },
    pbr:              { weight: 10, max: 1.5 },
    moat:             { weight: 10 }
  }.freeze

  def initialize(stock)
    @stock = stock
    @metrics = stock.latest_metric
  end

  def analyze
    scores = {
      roe: score_roe,
      debt_ratio: score_debt_ratio,
      operating_margin: score_operating_margin,
      profit_growth: score_profit_growth,
      per: score_per,
      pbr: score_pbr,
      moat: 5  # MVP에서는 기본값
    }

    total = scores.values.sum
    grade = case total
            when 80..100 then 'A'
            when 60..79  then 'B'
            when 40..59  then 'C'
            else 'D'
            end

    { total_score: total, breakdown: scores, grade: grade }
  end

  private

  def score_roe
    return 0 unless @metrics&.roe
    @metrics.roe >= 15 ? 20 : (@metrics.roe / 15.0 * 20).round
  end

  # ... 나머지 점수 계산 메서드
end
```

### 5.2. 프로필 계산기

```ruby
# app/services/profile_calculator.rb
class ProfileCalculator
  def initialize(user)
    @user = user
    @records = user.investment_records
    @surveys = user.survey_responses
  end

  def calculate
    survey_score = calculate_survey_score
    actual_score = calculate_actual_score
    biases = detect_biases(survey_score, actual_score)

    @user.investment_profile.update!(
      survey_score: survey_score,
      actual_score: actual_score,
      risk_tolerance: determine_risk_tolerance(actual_score),
      biases: biases
    )
  end

  private

  def detect_biases(survey, actual)
    biases = {}

    # 과잉 확신 감지: 높은 별점 + 손실
    if overconfidence_detected?
      biases[:overconfidence] = true
    end

    biases
  end
end
```

### 5.3. CSV 임포트

```ruby
# app/services/csv_import_service.rb
class CsvImportService
  def initialize(user, file)
    @user = user
    @file = file
  end

  def import
    results = { success: 0, failed: 0, errors: [] }

    CSV.foreach(@file.path, headers: true) do |row|
      record = build_record(row)
      if record.save
        results[:success] += 1
      else
        results[:failed] += 1
        results[:errors] << record.errors.full_messages
      end
    end

    results
  end

  private

  def build_record(row)
    stock = Stock.find_by(symbol: row['종목코드'])

    @user.investment_records.build(
      stock: stock,
      action: row['구분'] == '매수' ? 'buy' : 'sell',
      quantity: row['수량'].to_i,
      price: row['단가'].to_d,
      traded_at: Date.parse(row['거래일'])
    )
  end
end
```

---

## 6. 라우팅

```ruby
# config/routes.rb
Rails.application.routes.draw do
  # 인증
  resource :session, only: [:new, :create, :destroy]
  resource :registration, only: [:new, :create]

  # 관리자
  namespace :admin do
    root "users#index"
    resources :users, only: [:index, :show, :update] do
      member do
        patch :suspend
        patch :unsuspend
      end
    end
  end

  # 대시보드
  root 'dashboard#show'
  resource :dashboard, only: [:show]

  # 종목 분석
  resources :stocks, only: [:index, :show] do
    member do
      get :analyze
    end
  end

  # 투자 기록
  resources :investment_records do
    collection do
      post :import  # CSV 업로드
    end
  end

  # 성향 분석
  resource :profile, only: [:show, :edit, :update]
  resources :surveys, only: [:index, :create]

  # 헬스체크
  get '/health', to: 'health#show'
end
```

---

## 7. MVP 화면 구성

### 7.1. 대시보드 (`/dashboard`)

```
┌────────────────────────────────────────────────┐
│  WiseInvest                        [로그아웃]    │
├────────────────────────────────────────────────┤
│                                                │
│  ┌──────────────┐  ┌──────────────┐            │
│  │  투자 성향     │  │  최근 분석      │         │
│  │  보수적 (62)   │  │  삼성전자 B    │          │
│  │              │  │  SK하이닉스 A  │           │
│  └──────────────┘  └──────────────┘            │
│                                                │
│  ┌──────────────────────────────────────────┐  │
│  │  나의 투자 기록 요약                       │ │
│  │  총 15건 | 수익 +12.5%                    │ │
│  └──────────────────────────────────────────┘  │
│                                                │
└────────────────────────────────────────────────┘
```

### 7.2. 종목 분석 (`/stocks/:id`)

```
┌────────────────────────────────────────────────┐
│  삼성전자 (005930)                               │
├────────────────────────────────────────────────┤
│                                                │
│  버핏 스타일 점수: 72점 (B등급)                  │
│                                                │
│  ┌────────────────────────────────────────┐   │
│  │ ROE           15.2%    ████████░░ 18/20│   │
│  │ 부채비율        42%      ██████████ 15/15│   │
│  │ 영업이익률      12.3%    ██████████ 15/15│   │
│  │ 이익 증가       3년      ██████░░░░ 12/20│   │
│  │ PER           11.2    ████████░░  8/10│   │
│  │ PBR           1.2     ██████░░░░  6/10│   │
│  └────────────────────────────────────────┘   │
│                                                │
│  [내 관심 별점 주기] ★★★☆☆                  │
│                                                │
└────────────────────────────────────────────────┘
```

### 7.3. 투자 기록 (`/investment_records`)

```
┌────────────────────────────────────────────────┐
│  투자 기록                    [CSV 업로드] [추가]   │
├────────────────────────────────────────────────┤
│                                                │
│  날짜       종목      구분   수량   단가    별점 │
│  ─────────────────────────────────────────────│
│  01/10    삼성전자   매수   10   72,000   ★★★★   │
│  01/08    SK하이닉스 매수    5  180,000   ★★★    │
│  01/05    카카오     매도   20   52,000   -      │  
│                                                │
└────────────────────────────────────────────────┘
```

### 7.4. 성향 리포트 (`/profile`)

```
┌────────────────────────────────────────────────┐
│  나의 투자 성향 리포트                              │
├────────────────────────────────────────────────┤
│                                                │
│  ┌─────────────────┐  ┌─────────────────┐      │
│  │ 설문 기반 점수     │  │ 실제 행동 점수      │      │
│  │      72         │  │      58         │      │
│  │   (보수적)        │  │   (중립적)       │      │
│  └─────────────────┘  └─────────────────┘      │
│                                                │
│  ⚠️ 발견된 편향                                  │
│  ┌──────────────────────────────────────────┐ │
│  │ 과잉 확신: 높은 별점(4-5점)을 준 종목 중         │ │
│  │ 3건에서 손실이 발생했습니다.                    │ │
│  └──────────────────────────────────────────┘ │
│                                               │
│  💡 제안: 매수 전 버핏 점수가 60점 미만인 종목은        │
│     재검토해보세요.                                │
│                                                │
└────────────────────────────────────────────────┘
```

### 7.5. 관리자 - 사용자 관리 (`/admin`)

```
┌────────────────────────────────────────────────┐
│  사용자 관리                                      │
│  총 15명의 사용자가 등록되어 있습니다                   │
├────────────────────────────────────────────────┤
│                                                │
│  사용자          가입일       권한      상태         │
│  ─────────────────────────────────────────────│
│  홍길동           01/10    관리자     활성         │
│  (admin@...)                                  │
│  김투자           01/08    일반      활성         │
│  (investor@...)                               │
│  정지된사용자       01/05    일반      정지됨       │
│  (suspended@...)                              │
│                                                │
│                    [1] [2] [3]                │
└────────────────────────────────────────────────┘

┌────────────────────────────────────────────────┐
│  ← 사용자 상세                                    │
│  investor@example.com                          │
├────────────────────────────────────────────────┤
│                                                │
│  ┌─────────────────┐  ┌─────────────────┐      │
│  │ 기본 정보         │  │ 권한 관리         │      │
│  │ 이름: 김투자      │  │ 현재: 일반 사용자  │      │
│  │ 가입: 2024-01-08│  │ 상태: 활성        │      │
│  │ 기록: 5건        │  │                 │      │
│  │ 설문: 10건       │  │ [관리자 권한 부여]  │      │
│  │                 │  │ [계정 정지]       │      │
│  └─────────────────┘  └─────────────────┘      │
│                                                │
└────────────────────────────────────────────────┘
```

**관리자 기능:**
- 첫 번째 가입자 자동 관리자 지정
- 사용자 목록 조회 (페이지네이션)
- 관리자 권한 부여/해제
- 계정 정지/활성화
- 정지된 계정 로그인 차단

---

## 8. 외부 API (MVP)

### 8.1. KIS Developers

| 용도 | API | 비고 |
|------|-----|------|
| 종목 검색 | 국내주식 종목검색 | 종목코드/명 검색 |
| 현재가 | 국내주식 현재가 | 시세 조회 |
| 재무정보 | 국내주식 재무비율 | ROE, PER 등 |

### 8.2. 캐싱 전략

```ruby
# 간단한 캐싱 (Rails.cache 활용)
def fetch_stock_metrics(symbol)
  Rails.cache.fetch("stock_metrics:#{symbol}", expires_in: 24.hours) do
    KisClient.new.financial_ratios(symbol)
  end
end
```

---

## 9. 개발 순서

| 순서 | 작업                     | 예상 난이도 | 상태 |
|:----:|------------------------|:----------:|:----:|
| 1 | Rails 8 프로젝트 생성 + 인증  | 낮음 | 완료 |
| 2 | Stock, StockMetric 모델 + 시드 데이터 | 낮음 | 완료 |
| 3 | 종목 검색/조회 화면              | 낮음 | 완료 |
| 4 | BuffettAnalyzer 서비스 구현    | 중간 | 완료 |
| 5 | InvestmentRecord CRUD + CSV 업로드 | 중간 | 완료 |
| 6 | Survey + InvestmentProfile  | 중간 | 완료 |
| 7 | ProfileCalculator (편향 감지) | 중간 | 완료 |
| 8 | Dashboard 통합               | 낮음 | 완료 |
| 9 | 관리자 기능 (사용자 관리)        | 낮음 | 완료 |
| 10 | KIS API 연동                 | 높음 | - |

---

## 10. 기술 스택 요약

| 구분 | 기술 |
|------|------|
| Backend | Rails 8 |
| Frontend | Hotwire + Tailwind CSS |
| Database | PostgreSQL |
| Background | Solid Queue |
| Cache | Solid Cache |
| 배포 | Render (PaaS) |

---

## 11. 배포 (Render)

### 11.1. 아키텍처

```
┌─────────────────────────────────────────────────────┐
│                    Render.com                        │
├─────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────────────┐      ┌─────────────────┐       │
│  │   Web Service   │ ───▶ │   PostgreSQL    │       │
│  │   (Docker)      │      │   (Managed)     │       │
│  │                 │      │                 │       │
│  │  Rails 8 App    │      │  wiseinvest_db  │       │
│  │  + Thruster     │      │                 │       │
│  └─────────────────┘      └─────────────────┘       │
│           │                                          │
│           ▼                                          │
│  ┌─────────────────┐                                │
│  │  Persistent     │                                │
│  │  Disk (Storage) │                                │
│  └─────────────────┘                                │
│                                                      │
└─────────────────────────────────────────────────────┘
          │
          ▼
    ┌───────────┐
    │  GitHub   │ ──▶ 자동 배포 (Push 시)
    └───────────┘
```

### 11.2. 환경 변수

| 변수 | 설명 | 설정 방법 |
|------|------|-----------|
| `DATABASE_URL` | PostgreSQL 연결 | 자동 (Blueprint) |
| `RAILS_MASTER_KEY` | 암호화 키 | 수동 설정 필요 |
| `RAILS_ENV` | 환경 | `production` |

### 11.3. 배포 명령어

```bash
# Blueprint 배포 (권장)
# render.yaml 파일이 자동으로 인프라 구성

# 수동 배포 시 Render Shell에서:
rails db:migrate              # 마이그레이션
rails db:seed                 # 시드 데이터
rails runner "..."            # 관리자 생성
```

### 11.4. 비용 (2024년 기준)

| 서비스 | 무료 티어 | 유료 |
|--------|-----------|------|
| Web Service | 750시간/월 | $7/월 (Starter) |
| PostgreSQL | 90일 | $7/월 (Starter) |

> **참고**: 무료 티어는 15분 비활성 시 슬립 모드로 전환됨
